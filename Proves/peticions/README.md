## Instructions to run the server
- Install [Node](https://nodejs.org/) in your machine
- Run a terminal instance in the folder `/peticions`
- Run the command `npm i` to install dependences
- Run the command `npm start` to run the server
